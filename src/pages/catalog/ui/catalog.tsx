import styles from "./styles.module.scss"

const Catalog = () => {
    return (
        <section className={styles.catalogPage}>
            <h1>Catalog Page</h1>
        </section>
    )
}

export { Catalog }